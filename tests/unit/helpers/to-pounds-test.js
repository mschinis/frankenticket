import { toPounds } from '../../../helpers/to-pounds';
import { module, test } from 'qunit';

module('Unit | Helper | to pounds');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toPounds(42);
  assert.ok(result);
});
